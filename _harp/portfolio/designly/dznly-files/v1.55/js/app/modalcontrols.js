//To add additional modal controls, add them to the dependencies here
define([
    'app/views/filepicker',
    'app/views/embedder'
    ], 
    function(FilePickerControl, EmbedControl) {
    return arguments;
});